language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-h5slider' ]; then cd .. && eval "mv $currentfolder generator-h5slider" && cd generator-h5slider; fi
